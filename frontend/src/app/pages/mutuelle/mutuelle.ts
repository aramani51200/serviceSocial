import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-mutuelle',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './mutuelle.html',
  styleUrl: './mutuelle.css'
})
export class Mutuelle {
}